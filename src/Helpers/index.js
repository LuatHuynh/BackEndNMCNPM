const {
    genarateSalt,
    createHashPassword,
    validatePassword,
    GenerateToken,
    FormatID,
    VerifyAccessToken,
    formatData,

} = require('./dataProcess');


module.exports = {
    genarateSalt,
    createHashPassword,
    validatePassword,
    GenerateToken,
    FormatID,
    VerifyAccessToken,
    formatData,
    
}