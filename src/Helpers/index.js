const {
    genarateSalt,
    createHashPassword,
    validatePassword,
    GenerateToken,
    formatData,

} = require('./dataProcess');


module.exports = {
    genarateSalt,
    createHashPassword,
    validatePassword,
    GenerateToken,
    formatData,
    
}